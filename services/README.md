# Services

This directory is reserved for future Go services.

The first planned service is a storage gateway for:

- filesystem or NAS-backed storage
- provider-specific OAuth refresh logic
- webhook ingestion
- media processing jobs
